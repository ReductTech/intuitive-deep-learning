# 模拟

模拟必须显式说明假设、输入、输出、固定条件和与真实现象的差别；它展示“在这个模型下会怎样”，不是把模型结果冒充现实事实。让条件变化产生可比较的结果，必要时固定随机种子、保留前次轨迹或显示波动范围。

一次改变承担解释责任的变量，检查边界值、重置、重复运行和异常路径，确保图、数值、状态和文字来自同一计算。参数范围服务可解释的现象，不用无意义自由度制造噪声；随机波动、拟合和相关不自动变成确定机制。

教学简化、失效范围和仍未解决的不确定性就近说明。没有可比较的因果后果时，不为了“可玩”增加模拟。
