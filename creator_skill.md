# 课程创作 Skill

## 使命

把老师提供的主题、笔记、截图、教材页面、数据、论文、案例或一段描述，发展成一套真正能帮助学习者形成理解的 React 课程模块。

理想交付物拥有两种自然形态。

- 适合课堂投影与教师讲授的 1600×900 Web PPT
- 适合课后自主探索、回看和深入阅读的 Blog / Guide

两种形态共享同一条知识结构、认知旅程和核心状态，但不要求拥有完全相同的版式和信息密度。

课程成功的标准不是页面数量、组件数量、交互数量或视觉复杂度，而是学习者是否能够更快看见关键关系，更快形成预测，更快发现矛盾，更快建立解释，并最终能够用自己的话复述和迁移所学内容。

课程页面不是把内容放进网页，而是把一个认知过程设计成可被看见、可被理解、可被记住的界面。设计感不是附加装饰，而是让重点更突出、关系更清楚、理解更迅速的手段。

---

# 一、执行优先级

当不同要求之间发生冲突时，按以下优先级处理。

1. 老师明确要求与原始材料中的事实
2. 教学目标与认知正确性
3. 仓库当前 develop 分支的工程约束
4. Shared UI Kit 的语义、状态和可访问性规范
5. 页面可读性、稳定性与可访问性
6. 视觉设计与交互丰富度
7. 风格创新与装饰效果

不得为了视觉效果牺牲事实准确性。

不得为了交互丰富度牺牲认知清晰度。

不得为了模块个性绕开已有 Shared UI Kit。

不得为了填满页面、达到页数或制造科技感增加没有教学作用的内容。

---

# 二、工程事实来源

开发时以 develop 分支为实现基线。

当前项目中的事实来源优先级如下。

```text
当前 develop 代码与 AGENTS.md
→ modules/shared/react/ 当前实现
→ src/app/ 当前注册与路由
→ active module 当前实现
→ 模块 README、设计说明和历史文档
→ main 分支
→ modules-legacy/
```

`modules-legacy/` 只用于了解历史尝试，不作为 active module 的运行时代码、资源或设计模板。

现有 active module 也可能包含迁移期实现。研究它们的教学思路和成熟经验，但不要假定每一处代码都是当前最佳实践。

特别是 `modules/Neuron-Guide`。

请把它当作设计研究对象，而不是模板。

学习它如何把抽象知识变成可观察、可操作、可讨论的过程，学习它对节奏、叙事、状态变化、反馈和信息密度的判断。

不要机械复制它的页面结构、色彩、卡片样式、章节顺序、交互形式或模块私有实现。

---

# 三、开始创作前先理解课程

不要看到主题后立即开始写 JSX。

首先理解这门课真正要发生什么。

输入可能十分完整，也可能只有一张截图、一段口述或几个关键词。先提取以下信息。

## 3.1 已确认事实

包括材料直接给出的事实、公式、数据、定义、人物、事件和结论。

这些内容不得随意改写成不同事实。

## 3.2 核心关系

寻找课程中最值得被看见的关系。

例如。

- 一个变量如何影响另一个变量
- 一个过程如何随时间变化
- 两种方法的差别在哪里
- 一个模型为什么失败
- 一个反例为什么打破直觉
- 一个公式如何对应可观察现象
- 一个历史事件如何改变后续发展
- 一个复杂系统如何从局部行为形成整体结果

## 3.3 学习障碍

判断学习者最可能在哪里卡住。

例如。

- 把相关性误认为因果
- 只会代公式但不知道变量代表什么
- 记住结论但看不出为什么
- 把两个相近概念混在一起
- 看到二维图后无法理解高维情况
- 认为一个反例推翻整个模型
- 只关注数值大小而忽略评价目标

## 3.4 视觉线索

判断哪些内容天然适合成为。

- 散点图
- 时间轴
- 数轴
- 流程
- 档案
- 地图
- 网络
- 对照实验
- 矩阵
- 状态机
- 模拟
- 分布
- 几何结构
- 动态过程
- 局部放大
- 前后对比

## 3.5 不确定内容

区分。

- 材料中明确给出的事实
- 为教学而设计的示意例子
- 根据材料做出的设计推断
- 仍需老师确认的关键内容

只有会显著改变课程方向、事实正确性或教学目标的不确定性需要阻塞并请求确认。

不会改变核心方向的细节，应做出合理、保守且可解释的设计判断并继续推进。

---

# 四、建立教学 Brief

在页面设计之前，先形成一个简洁的教学 Brief。

至少明确以下内容。

```text
目标学习者
已有知识
授课时长
学习目标
必须保留的事实
核心认知障碍
最终希望学习者能够做什么
适合的课程视觉隐喻
需要真实证据支持的内容
仍需老师确认的关键问题
```

Brief 不需要写成长篇报告。

它的作用是保证后续页面都服务于同一个教学目标。

---

# 五、先设计认知地图，再设计页面

不要从“第一页做什么布局”开始。

先设计学习者的理解如何发生变化。

推荐用以下形式建立一条简洁的认知地图。

```text
学习者当前理解
↓
出现新的证据或现象
↓
原有解释出现不足
↓
引入新的概念或模型
↓
新概念解释现象
↓
通过反例、比较或实验检验
↓
学习者形成新的判断
↓
迁移到新的问题
```

对于重要概念，优先让学习者经历。

```text
看见
→ 猜测
→ 比较
→ 解释
→ 应用
→ 回顾
```

这不是必须机械执行的固定模板。

如果一个概念本身非常基础，直接解释比预测更有效，可以直接解释。

如果历史事实本身就是证据，不需要虚构互动。

如果公式只是对已经理解关系的压缩，应先让关系可见，再出现公式。

---

# 六、页面大纲

认知地图确定后，再形成课程页面大纲。

典型 30 至 60 分钟课程可以把 18 至 28 个课程单元作为初始估计，但最终页数由认知步骤决定。

禁止为了达到目标页数拆分页面或填充内容。

禁止把一个自然连续的认知步骤强行拆成多个页面。

禁止因为内容丰富而把多个独立问题塞进同一页。

每个页面只需要先回答四个问题。

```text
这一页的主问题是什么
学习者首先应该注意到什么
这一页提供什么新的证据或理解
它为什么自然通向下一页
```

不需要在大纲阶段预先指定统一组件或布局。

---

# 六点五、单页优先设计

默认一次只设计一页。

除非老师明确要求，或多页之间存在强依赖关系，不要一次性生成整套课程，也不要一次性铺开多个页面的完整代码。

单页优先的原因不是降低效率，而是保证每一页都被单独思考、单独构图、单独验证。课程页面不是把同一种布局重复二十次，而是让每一页围绕当前认知任务被认真设计。

在开始实现某一页之前，必须先明确：

```text
这一页的主问题是什么
这一页最重要的视觉证据是什么
学习者第一眼应该看哪里
这一页的版面为什么适合这个内容
这一页与前一页、后一页如何衔接
```

当老师要求继续时，再进入下一页。

如果当前页尚未达到较高完成度，不要急于生成后续页面。

对于一套课程，允许不同页面拥有不同的布局方式、视觉重心和构图策略。不要因为已经完成了一页，就默认后续页面沿用同一套结构。

---

# 七、课程视觉隐喻

每门课程都应形成自己的界面性格。

视觉形式由知识本身决定。

例如。

数学课可以像实验台、坐标纸、推演纸、测量仪器或空间地图。

历史课可以像档案馆、报纸、时间轴、展览或人物关系网。

编程课可以像调试现场、日志、运行时状态、机器内部或数据流。

生物课可以像观察台、显微镜、组织结构或信号网络。

统计课可以像调查现场、证据板、分布地图或实验记录。

视觉隐喻必须能够解释页面为什么长成这样。

如果一个页面换成其他课程标题后仍然完全成立，它很可能只是通用模板。

---

# 七点五、视觉构图与层次

课程页面不是文档排版，而是围绕知识组织的一次视觉构图。克制不等于平淡，清晰不等于只有标题和文字。页面必须同时做到有设计感、有层次、有重点，并让学习者快速抓住最重要的信息。

## 7.5.1 每页必须有视觉重心

除纯过渡页外，每一页都应存在一个明确的视觉主角。它可以是：

- 一张关键图表
- 一幅关系图
- 一个核心公式与图形组合
- 一张历史图片或档案材料
- 一个对比结构
- 一个局部放大的重点对象
- 一个实验或模拟区域

不要让页面变成数个权重接近的小块平均铺开。也不要让标题、正文、卡片和按钮都处在同一视觉重量上。

学习者在三秒内应能看出：

```text
哪里最重要
哪里是解释
哪里是补充
```

## 7.5.2 建立明显的三级层次

页面至少形成以下三个层次：

1. 主层：当前最重要的证据、图形或问题，承担主要注意力。
2. 支撑层：对主层进行解释、比较、补充或局部说明。
3. 注释层：标签、来源、变量解释、脚注、提示等低优先信息。

不要让所有内容面积接近、字号接近、颜色接近、边框样式接近。没有层次的页面通常会显得像模板或表单，而不是课程。

## 7.5.3 用构图建立理解，不用模板填空

页面布局应由认知任务决定，而不是从预设模板开始。

例如：

- 讲趋势时，图表应成为主视觉，文字围绕图表服务。
- 讲历史背景时，历史材料、图片、时间信息和研究问题应形成叙事构图。
- 讲公式推导时，应优先保证推导路径清楚、变量对应明确，而不是机械分栏。
- 讲对比时，应使用同一参照系下的并置或前后结构，而不是拆成彼此孤立的小卡片。

不要默认使用相同的二栏布局、三卡片布局、上下标题正文布局。允许不同页面有不同构图，但必须保持阅读顺序明确。

## 7.5.4 视觉丰富度来自有效信息，而不是装饰

页面可以精致、丰富、有设计感，但丰富度应来自：

- 图文关系
- 尺度差
- 主次分层
- 材质与模块对比
- 图表与说明的配合
- 真实材料与教学结构的结合
- 局部放大、对比、标注和状态变化

不要依赖以下方式制造“设计感”：

- 过多渐变
- 发光、光圈、流光
- 随机装饰图形
- 没有教学作用的插图
- 大量样式相同的卡片
- 花哨但无意义的动画

精致应表现为：间距舒服、主次清楚、对齐严格、留白有目的、重点明确、信息密度合理。

## 7.5.5 空白必须服务于聚焦

留白不是把内容做少，而是为了让重点更集中，让不同信息块之间形成明确边界。

允许使用大面积空白，但必须能解释它的作用，例如：

- 强调唯一主体
- 为重要图形留出观察空间
- 将主要论点与次级注释分开
- 形成停顿和阅读节奏

如果空白只是因为内容被缩在角落，或整页只有几个零散模块没有形成整体构图，则说明页面尚未设计完成。

## 7.5.6 优先把关系画出来

只要某个关系可以被可视化，就优先可视化，而不是完全依赖文字解释。

优先转化为视觉的内容包括：

- 因果或影响关系
- 数量变化
- 趋势与分布
- 时间顺序
- 结构组成
- 方法对比
- 错误与修正
- 输入与输出
- 真实值与预测值
- 局部与整体

不要让页面出现长段文字解释一个本可以直接画出来的关系。

## 7.5.7 让教学原则在界面中可见，而不是停留在说明里

认知负荷、信号原则、空间邻近、生成效应、脚手架等原则不需要在页面上被说出来，但必须在页面中被体现出来。

也就是说，好的页面应能看出：

- 只有一个真正主问题
- 最重要的内容最先被看见
- 图和解释是贴近的
- 颜色承担了稳定语义
- 学习者被引导先观察、再判断、再理解
- 页面中存在清楚的下一步

如果这些原则只能在 skill 文本里看到，而不能在界面中看出来，说明页面设计仍然停留在排版层面。

课程个性不是通过随机颜色、渐变、玻璃拟态或大量装饰制造出来的，而是通过知识本身的结构形成。

---

# 八、认知与教学设计原则

## 8.1 一页一个主问题

每页只保留一个占主导地位的问题或判断。

辅助信息可以存在，但不能与主问题争夺注意力。

## 8.2 分段

复杂推导、流程和实验拆成可理解的小段。

不要一次性展示完整推导、全部参数、全部结论和全部解释。

## 8.3 信号

颜色、位置、动画顺序和标注都必须承担语义。

颜色不要随机装饰。

同一种颜色在同一课程中尽量保持一致语义。

## 8.4 空间邻近

变量说明靠近公式。

图例靠近图表。

解释靠近被解释的元素。

视觉反馈靠近触发变化的操作。

## 8.5 多媒体分工

图表负责展示关系和变化。

文字负责命名、解释、提出问题和作出判断。

公式负责压缩已经建立的关系。

交互负责产生静态页面无法直接提供的新证据。

来源和数据负责可信度与追溯。

不要把同一段内容原样重复在标题、图中和正文里。

## 8.6 即时解释

术语、符号和变量第一次真正需要时再解释。

不要为了“完整”在开头提前堆积大量定义。

## 8.7 生成效应

正式答案出现前，在合适场景优先让学习者。

- 猜趋势
- 估计结果
- 选择假设
- 判断方向
- 预测变化
- 给出解释

## 8.8 检索练习

阶段结束时优先让学习者。

- 回忆
- 复述
- 排序
- 补全
- 比较
- 迁移

而不是只重新阅读结论。

## 8.9 自我解释

问题不仅问答案是什么。

也可以问。

- 你根据哪条证据判断
- 为什么会发生这种变化
- 哪个因素真正决定结果
- 为什么另一个模型在这里更差

## 8.10 概念改变

当一个常见直觉不准确时，优先安排一个能暴露问题的反例或实验。

不要只把错误答案标成错误。

## 8.11 脚手架与渐隐

第一次可以提供完整图例、例子和变量说明。

后续逐渐撤掉提示。

让学习者承担更多推理。

## 8.12 交错对比

相近概念优先放在同一组证据上比较。

例如。

MAE 与 MSE 使用同一组误差。

Sigmoid 与 Softmax 使用同一组分类场景。

冻结特征与端到端训练使用同一任务结果。

---

# 九、交互判断

交互不是完成度指标。

滑杆、按钮、动画、弹窗、拖拽和选择框只有在它们改变学习者能够观察到的证据时才值得存在。

每一个交互都应能够回答以下问题。

1. 学习者操作前应该猜什么
2. 操作改变哪个可观察量
3. 变化如何反馈一个概念
4. 操作结束后学习者应该能解释什么

如果无法回答这些问题，优先取消交互。

## 9.1 交互比例

带实质交互的页面默认不超过整套课程的一半。

其余页面可以是。

- 真实背景
- 历史证据
- 观察图
- 公式推导
- 连续解释
- 对比
- 反例
- 案例
- 总结
- 过渡
- 延伸阅读

## 9.2 无交互页

无交互页不需要人为制造“完成”按钮。

当学习者只需要观察、阅读或理解时，优先使用 Shared LessonFlow 的流程能力。

例如。

```text
revealMode: 'cue'
revealMode: 'scroll'
revealMode: 'immediate'
```

或者使用当前 Shared UI Kit 中适合的 ScrollCue / ProgressiveReveal 等能力。

不要为了推进流程添加没有认知作用的按钮。

对于阅读型步骤，严格区分“滚动提示”和“继续按钮”：如果步骤应由向下滚动、触摸上滑或键盘推进，应复用 `LectureAdvanceCue`（或 Shared 中等价的滚动提示能力），只显示轻量的滚动提示，不额外渲染“继续”按钮。只有在学习者必须明确确认、提交答案或完成一次操作时，才使用按钮。

## 9.3 允许的专用交互

当 UI Kit 无法表达课程特有操作时，可以实现模块专用交互。

例如。

- 画布绘制
- 棋盘格选择
- 图表图例操作
- 图像标注
- 拖动几何点
- 模型结构操作
- 时间轴拖动
- 专用模拟器

这些控件仍需遵循 Shared token、焦点状态、键盘访问和可访问性要求。

---

# 十、同一知识的多种表达

同一个课程单元可以拥有。

- PPT
- Guide
- 图表
- 公式
- 视频
- 动画
- 实验
- 来源
- 详细解释

但这些内容必须指向同一个概念骨架。

不要分别讲一套。

例如一个残差概念。

```text
图中
真实值与预测值之间的垂直线

文字中
真实值与预测值的差

公式中
y - ŷ

实验中
移动预测线后残差长度变化
```

它们必须互相对应。

---

# 十一、PPT 与 Guide

PPT 与 Guide 共享。

- 教学目标
- 核心知识结构
- 课程单元
- 关键证据
- 领域计算
- 共享状态
- 学习顺序

但不要求共享完全相同的信息展开方式。

## PPT

PPT 优先服务于。

- 教师讲授
- 集体注意力
- 大屏观看
- 一眼理解
- 明确的视觉焦点

一个典型 PPT 单元可以是。

```text
一个主问题
一个核心视觉证据
一句关键解释
必要时一个交互
```

## Guide

Guide 优先服务于。

- 自主阅读
- 回看
- 展开
- 深入解释
- 来源追溯
- 延伸实验

同一个单元在 Guide 中可以进一步拥有。

```text
核心视觉
简短解释
可展开正式定义
公式
数据来源
实验
延伸阅读
```

默认视图不要同时展开所有深度层次。

---

# 十二、课程单元必须拥有单一事实源

不要分别维护三套课程顺序。

Guide、PPT 和开发预览应尽量来自同一组课程单元定义。

推荐思路。

```text
lesson unit definitions
        ↓
Guide LessonFlow
        ↓
PPT slide definitions
        ↓
block preview / route metadata
```

课程单元至少可以包含。

```ts
id
title
section
description
revealMode
render
ppt
guide
```

实际结构根据项目需要设计，不要求机械使用这一接口。

重点是。

- 标题不要在三处重复维护
- 顺序不要在三处重复维护
- description 不要手工复制
- Guide 与 PPT 不要悄悄漂移
- dev preview 应尽量由课程单元定义派生

当课程结构发生变化时，应能够在一个主要位置完成修改。

---

# 十三、Shared UI Kit 是工程硬约束

实现前先查看当前 develop 分支的。

```text
modules/shared/react/index.ts
```

以及 Shared UI Kit 对应实现。

Skill 中列出的组件只是核心示例，不代表完整 API。

Shared UI Kit 会继续演进。

凡是 Shared 已经提供的能力，优先直接使用。

当前常见能力包括但不限于。

## 控件

```text
Button
TextInput
RangeControl
Switch
Select
ExplainPanelButton
```

## 布局

```text
ModuleShell
LessonStage
ContentBlock
CatalogItem
```

## 反馈

```text
Callout
NoticeStrip
Feedback
ReplayableCallouts
AttentionHint
```

## 学习组件

```text
Question
ValueTile
FormulaBlock
MathFormulaBlock
RelatedVideos
LessonFooter
PageRating
ProgressiveReveal
ScrollCue
PanelChoiceQuestion
CodeCompletionBlock
LessonFlow
```

## 可视化

```text
FunctionPlot
EChartsChart
PlotlyChart
```

具体以当前 `modules/shared/react/index.ts` 为准。

---

# 十四、Typography 规则

独立教学文本使用 Shared `Typography`。

包括。

- 页面标题
- 小节标题
- 解释文字
- 标签
- 独立公式说明
- 状态说明
- 辅助信息

Shared 组件内部自己的文字由 Shared 组件负责，不要求外层再次包 Typography。

以下内容可以根据渲染方式使用专用文本。

- SVG 内坐标轴
- Canvas 内标注
- 图表刻度
- 图表内部标签
- 公式渲染器
- 领域专用可视化

这些文字仍应尽量遵循 Shared token 和课程整体视觉等级。

模块 CSS 不得重新定义 Shared Typography 的基础。

- 字体
- 字号
- 字重
- 行高

需要变化时优先使用现有。

```text
variant
tone
align
wrap
className
token
```

---

# 十五、什么时候不应该创建新组件

创建模块专属组件前，依次判断。

1. Shared UI Kit 是否已有等价能力
2. Shared primitives 是否可以组合实现
3. 这个组件是否承载本课程特有知识关系
4. 它是否会在多个 block 中重复使用
5. 它是否具有清晰独立职责

如果只是为了改变。

- 按钮外观
- 普通卡片外观
- 标题外观
- 基础表单
- 普通提示框

不要创建模块专属组件。

避免产生没有真实领域职责的。

```text
FancyButton
InfoCard
ConceptCard
LessonCard
PrettyPanel
CustomSlider
```

只有课程知识本身需要的可视化和交互才留在模块目录。

---

# 十六、模块目录

每门主动开发课程位于。

```text
modules/<ModuleName>/
```

新课程的页面、blocks、components、data、model、services 与 assets 必须直接位于自己的 `modules/<ModuleName>/` 目录下，不得挂载到另一个课程模块目录内部。

推荐按需使用。

```text
modules/<ModuleName>/

├─ <ModuleName>Page.tsx
├─ <ModuleName>PptSlidePage.tsx
├─ <ModuleName>.css

├─ blocks/
│  ├─ OpeningBlock.tsx
│  ├─ ...
│  └─ CompletionBlock.tsx

├─ components/
│  ├─ Plot.tsx
│  ├─ Diagram.tsx
│  └─ ...

├─ assets/
├─ data/
├─ model/
├─ services/
└─ README.md / sources.md
```

不要为了目录完整创建空文件夹。

## blocks

服务教学流程。

一个 block 通常表达一个主问题或认知阶段。

不要把整门课程塞进一个巨大页面文件。

## components

服务视觉或交互复用。

不负责偷偷维护课程顺序。

不应单独持有跨 block 的学习进度。

## data

保存可以审阅的。

- 原始事实
- 数据
- 静态样本
- 示例数据
- 常量

明确区分真实数据与教学示意数据。

## model

保存知识如何计算。

例如。

- 数学公式
- 参数
- 派生指标
- reducer
- Context
- Provider
- 状态类型

页面不要重复实现同一公式。

## services

保存具有副作用的事情。

例如。

- 异步加载
- 本地缓存
- API
- LLM 评估
- 数据请求

没有副作用时不要为了目录结构创建 services。

## assets

保存本模块运行时需要的资源。

真正跨课程共享的资源才放进 `modules/shared/`。

禁止从 `modules-legacy/` 加载运行时资源。

---

# 十七、状态与课程流程

多个课程单元共享状态时，优先使用模块 Provider。

课程应考虑。

- 初始状态
- hydrate
- 用户操作后的状态
- 错误
- loading
- reset
- revisit
- persistence

不要让每个 block 独立维护同一份领域状态。

LessonFlow 负责课程流程时，block 只负责报告自己的完成状态，不应自行管理整套课程可见性。

如果 Shared 已经提供流程能力，不重复实现一套 wheel、touch、keyboard 推进逻辑。

---

# 十八、素材、来源与可信度

真实人物、历史事件、研究结论、数据集和外部媒体必须承担明确教学作用。

它们应该帮助回答问题，而不是只增加真实感。

使用外部素材时。

- 保存到对应模块目录
- 不依赖运行时远程地址
- 记录作者和来源
- 记录许可或访问日期
- 优先公共领域、开放许可或项目自有素材

如果没有合适外部素材，可以使用。

- SVG
- CSS
- Canvas
- 数据可视化
- AI 生成插图

并在 README、sources.md 或注释中说明用途和来源。

## 18.x 视觉素材补充

当课程背景、历史叙事、真实对象、实验场景、地理位置、人物、装置、档案、显微图像、结构示意或空间关系能够显著提升理解时，可以主动联网搜索、下载或生成视觉素材。

允许使用以下来源补充页面视觉证据：

- 公开可用的图片、档案、地图、照片和示意图
- 开放许可或可合理使用的历史材料
- 自行绘制的 SVG、图解和数据可视化
- AI 生成的背景插图、结构示意图或辅助性视觉素材

使用这些素材时应遵循以下原则：

1. 素材必须承担教学作用。它应帮助学习者理解背景、定位对象、形成比较、建立证据或增强观察，而不是仅用于装饰。
2. 优先使用真实材料。当真实图片、历史档案或实物照片能够提供更强证据时，优先使用真实材料。
3. 当真实素材不足时，可生成辅助视觉。例如生成风格统一的背景图、简洁插图、结构示意、局部氛围素材等，但不要让生成图替代本应由图表、公式或文字承担的精确信息。
4. 页面中的文字和关键数据尽量保持为可选择的网页元素。背景、纹理、辅助视觉和装饰性素材可以使用图片；关键说明、标签、图表、数字、公式和结论应优先保持为 DOM、SVG 或可交互组件。
5. 素材风格应服务于课程整体气质。不要一页写实、一页卡通、一页科技海报、一页极简表单。整套课程中的视觉风格应保持节制、统一和可信。

教学示意数据不得伪装成原始数据。

---

# 十九、PPT 画布

Web PPT 统一按照。

```text
1600 × 900
```

设计。

页面内容必须自然收进画布。

## 必须满足

- Grid / Flex 子项可收缩
- 使用 `min-width: 0`
- 需要时使用 `max-width: 100%`
- 网格优先使用 `minmax(0, 1fr)`
- 媒体不得撑破父容器
- SVG 不得撑破父容器
- Canvas 不得撑破父容器
- 公式不得撑破父容器
- 交互控件不得撑破父容器

## 禁止

- 用多个固定 `min-width` 拼一整行
- 用 `overflow-x: auto` 修复 PPT
- 依赖父元素 clipping 掩盖越界
- 为了塞下内容把所有文字缩得极小
- 依赖自动 scale 长期掩盖信息密度问题

自动缩放只允许作为渲染安全兜底。

如果页面只有明显缩小才能放进 1600×900，说明页面信息密度或布局存在问题，应重新设计。

---

# 二十、内容密度

克制不等于空。

一页不能只放一句结论和一个没有意义的交互撑满整个屏幕。

整套课程应根据主题合理包含。

- 真实背景
- 核心问题
- 关键概念
- 具体例子
- 对比
- 反例
- 常见误解
- 实验
- 预测
- 练习
- 阶段总结
- 延伸阅读

但每页仍然只服务一个主问题。

内容是否足够，不以字数判断，而以认知任务是否完整判断。

---

# 二十一、单页验收

每一页完成后，检查以下问题。

```text
1. 这一页唯一的主问题是什么
2. 学习者第一眼应该看哪里
3. 页面提供了什么新的证据
4. 图、文字、公式和交互分别承担什么职责
5. 是否存在删掉也不影响理解的元素
6. 如果有交互，操作前学习者预测什么
7. 操作改变哪个可观察量
8. 学习者完成后应该能说出什么
9. 这一页为什么自然通向下一页
10. 1600×900 下是否完整可见
```

如果第 3、8、9 项无法清楚回答，这一页通常需要重新设计，甚至删除。

另外，还应增加以下视觉验收：

```text
11. 页面是否存在明确视觉重心
12. 主层、支撑层、注释层是否清楚区分
13. 版面是否像为这一页内容专门设计，而不是套模板
14. 图表、文字、说明、标签之间是否形成紧密关系
15. 留白是否有明确作用，而不是内容稀疏
16. 页面是否具有足够层次感和精致感
17. 学习者是否能在短时间内抓住重点
```

如果第 11、12、13、17 无法肯定回答，则该页面通常仍然停留在普通排版层面，需要重新构图。

---

# 二十二、整套课程验收

整套课程完成后检查。

## 教学

- 是否有一条清晰认知旅程
- 是否有不必要重复页面
- 是否存在概念跳跃
- 是否适时使用反例
- 是否让学习者产生自己的判断
- 是否有检索或迁移机会
- 公式是否对应可观察关系

## 视觉

- 是否存在明确视觉隐喻
- 视觉形式是否由知识解释得通
- 是否避免模板化 SaaS 页面
- 是否避免无意义科技感
- 颜色是否承担语义
- 阅读顺序是否明确

## 交互

- 交互是否少而有力
- 每次操作是否带来新证据
- 是否存在假按钮
- 是否存在为了丰富页面添加的滑杆或弹窗
- 键盘操作和禁用态是否合理

## 工程

- 是否大幅复用 Shared UI Kit
- 是否使用当前 Shared API
- 是否从 modules-legacy 引入内容
- 是否正确使用 Provider
- 是否处理 hydrate、error、loading、reset 和 revisit
- 是否存在重复领域计算
- Guide、PPT、preview 是否来自一致课程结构

## PPT

- 是否完全在 1600×900 内
- 是否没有横向滚动
- 是否没有隐式裁切
- 是否不存在明显自动缩小
- 是否没有极小文字
- 是否所有媒体自然收进画布

## 来源

- 哪些是原始材料事实
- 哪些是外部资料
- 哪些是教学示意
- 哪些是设计推断
- 哪些仍需老师确认

---

# 二十三、常见失败模式

禁止出现以下行为。

## 教学失败

- 看到主题后立即生成 20 至 30 页代码
- 从知识点列表直接映射成 PPT 页
- 一页同时介绍多个新概念
- 直接给结论而没有必要证据
- 公式第一次出现时没有变量解释
- 图和公式互相没有对应关系
- 每页都必须互动
- 错误答案只显示错误而不解释原因
- 阶段总结只是重复正文

## 视觉失败

- 默认 Hero + 三卡片 + 三列布局
- 所有课程都长得一样
- 大量渐变和发光制造科技感
- 为了好看加入无教学作用插图
- 用随机颜色装饰
- 用卡片数量表现内容丰富
- 用阴影和圆角替代信息层级
- 用同一种布局连续生成多页
- 每一页都是相同的标题区、内容区和说明区
- 版面像文档或表单，而不是课程页面
- 所有内容块尺寸接近，缺少明显主次
- 只有文字和边框，没有真正的视觉证据
- 图表存在，但没有成为视觉主角
- 视觉风格过于单薄，像“白底加几个框”
- 页面缺少设计完成度，看不出被认真构图

## 交互失败

- 每页都有按钮
- 无交互页添加“继续”假按钮
- 滑杆改变了数值但没有概念反馈
- 点击后只反馈成功失败
- 动画只负责热闹
- 复杂交互没有 reset
- 交互结果无法用语言解释

## 工程失败

- 重写 Shared Button
- 重写 Shared RangeControl
- 原生表单替代已有 Shared 控件
- 在模块 CSS 中修改 Typography 字号
- 从 modules-legacy 引入资源
- 在多个 block 重复写同一公式
- 在 Guide 和 PPT 分别维护两份课程顺序
- 为了目录完整制造空 services / assets
- 一个巨大组件包含整门课程
- 为简单视觉效果创建大量无意义组件

## PPT 失败

- 依赖 overflow hidden 掩盖问题
- 用横向滚动修复
- 自动缩放后文字明显过小
- 多个固定像素列相互挤压
- 图表、公式或 Canvas 超出画布
- 为了塞内容缩小所有文字

---

# 二十四、协作方式

当老师正在和智能体交互式创作时，优先按照以下节奏合作。

```text
理解材料
→ 教学 Brief
→ 认知地图
→ 页面大纲
→ 小批量实现
→ 老师反馈
→ 调整
→ 下一批
```

不要在核心方向仍不明确时一次性生成整套页面。

但如果老师明确要求直接完成，或当前没有必要等待反馈，则基于已有材料做最合理的设计判断并继续执行。

不要因为小问题反复阻塞任务。

设计推断可以在交付时明确说明。

---

# 二十五、实现步骤

实际执行课程创作任务时，建议采用以下流程。

## Stage 0 材料理解

输出或内部形成。

```text
已确认事实
核心关系
主要学习障碍
视觉机会
不确定内容
```

此阶段不要直接生成整套课程。

## Stage 1 教学 Brief

明确。

```text
目标学习者
已有知识
时间
学习目标
最终能力
关键事实
视觉隐喻
```

## Stage 2 认知地图

建立。

```text
当前理解
→ 新证据
→ 理解变化
→ 下一问题
```

## Stage 3 页面大纲

为每页写。

```text
主问题
核心证据
预期理解
下一步
是否需要交互
```

## Stage 4 架构检查

实现前检查。

```text
AGENTS.md
modules/shared/react/index.ts
现有 Shared UI Kit
相关 active module
当前路由和注册方式
```

## Stage 5 单页实现

默认一次只实现一页。

先把当前页做到较高完成度，再进入下一页。只有在老师明确要求，或多个页面共享强依赖关系、必须同时调整时，才允许一次实现多页。

实现当前页时，应同时完成以下事项：

- 页面构图
- 核心视觉证据
- 文字与图表关系
- 必要的素材补充
- 交互或动效判断
- 1600×900 画布验证

不要为了尽快看到“很多页”，而牺牲单页设计质量。

## Stage 6 实际验证

运行课程并实际操作。

检查。

- 初始
- 操作
- 错误
- loading
- 完成
- reset
- revisit
- PPT

## Stage 7 教学复盘

删掉。

- 重复解释
- 无意义交互
- 多余装饰
- 不必要控件
- 不必要页面

---

# 二十六、最终判断标准

一个优秀课程模块应该同时满足以下条件。

它不能只是信息排版。

它不能只是课本变网页。

它不能只是静态 PPT 加几个按钮。

它不能只是一个通用 UI 模板替换标题。

它应该能够回答。

```text
学习者此刻正在观察什么
他下一步会产生什么预测
什么证据会改变他的理解
为什么这一页现在出现
为什么这个交互值得存在
为什么这个视觉形式属于这门课
这一页结束后他能否自己解释
```

最好的模块不是最复杂的模块。

也不是最像 Neuron-Guide 的模块。

而是只有这门课才能自然长成的模块。

Shared UI Kit 提供统一的语言、状态和可访问性。

课程模块负责表达知识本身独特的结构。

最终目标始终只有一个。

让学习者更快形成真正的理解。
